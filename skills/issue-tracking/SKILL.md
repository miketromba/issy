---
name: issue-tracking
description: Project issue tracking system. Use when creating, reading, updating, listing, searching, prioritizing, closing, reopening, or choosing project issues, tasks, bugs, improvements, or roadmap work.
---

# Learn issy from the CLI

When this skill is relevant, run:

```bash
issy learn
```

Follow the instructions printed by `issy learn`. That output is the canonical agent reference for the installed issy version.

If `issy learn` is unavailable because the CLI is not installed, ask the user to install `issy` first, then rerun `issy learn`.

For focused context, use:

```bash
issy learn topics
issy learn <topic>
```

Do not rely on this skill file for operational details. It is only a bootstrap adapter that points agents to `issy learn`.
